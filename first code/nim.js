"use strict";
let s = prompt("Please enter a number between 1 and 7. It will be applied to the first row", "") || "0";
let a = parseInt(s); // Convert the input to an integer
// if (isNaN(a) || a < 1 || a > 7) {
//     alert("Invalid input. Please enter a number between 1 and 7.");
//     throw new Error("Invalid input");
// }
let t = prompt("Please enter a number between 1 and 7. It will be applied to the first row", "") || "0";
let b = parseInt(s); // Convert the input to an integer
// if (isNaN(a) || a < 1 || a > 7) {
//     alert("Invalid input. Please enter a number between 1 and 7.");
//     throw new Error("Invalid input");
// }
let u = prompt("Please enter a number between 1 and 7. It will be applied to the first row", "") || "0";
let c = parseInt(s); // Convert the input to an integer
// if (isNaN(a) || a < 1 || a > 7) {
//     alert("Invalid input. Please enter a number between 1 and 7.");
//     throw new Error("Invalid input");
// 
let v = prompt("Please enter a number between 1 and 7. It will be applied to the first row", "") || "0";
let d = parseInt(s); // Convert the input to an integer
// if (isNaN(a) || a < 1 || a > 7) {
//     alert("Invalid input. Please enter a number between 1 and 7.");
//     throw new Error("Invalid input");
// }
//# sourceMappingURL=nim.js.map